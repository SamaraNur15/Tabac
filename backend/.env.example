# Configuración de Base de Datos
MONGO_URI=TU_MONGO_URI_AQUI
MONGO_DB=TabacWeb

# Puerto del servidor
PORT=3000

# URL del Frontend (para CORS)
FRONTEND_URL=https://tu-frontend-en-produccion

# JWT Secret (genera uno único con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=TU_JWT_SECRET_AQUI

# JWT Expiration
JWT_EXPIRES_IN=7d
